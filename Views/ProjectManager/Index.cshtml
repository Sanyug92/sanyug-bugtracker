@model IEnumerable<sanyug_bugtracker.Models.ApplicationUser>



@{
    ViewBag.Title = "Developers";
}

<h2>Developers</h2>

<p>

</p>
<table class="table">


    <tr>
        <th>
            Frist Name
        </th>
        <th>
            Last Name
        </th>
        <th>
          Projects
        </th>
    </tr>
    @if (Model != null)
    {
        foreach (var user in Model)
        {
         
    <tr>
            <td>   
              @Html.DisplayFor(modeluser => user.FirstName)   
                                     
            </td>
            <td>
                @Html.DisplayFor(modeluser => user.LastName)
            </td>
        <tb>
            @*@foreach (var item in Model.Tickets)

            {
                @Html.AntiForgeryToken()

                @Html.DisplayFor(modelItem => item.Title)


            }*@
        </tb>

        <td>
            @*@foreach(var item in user.roles) {
                @item <br/>
            }*@
            @*@Html.LabelFor(modeluser => user.user.Id, htmlAttributes: new { @class = "control-label col-md-2" })
            @Html.HiddenFor(User => user.user.Id)*@   
        </td>           u
            <td>              
               @Html.ActionLink("Assign Ticket", "AssignTicket", new {id = user.Id } )
            </td>
        </tr>
    }
}


</table>
